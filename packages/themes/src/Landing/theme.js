import { colors, space, typography, body, zIndex, breakpoints, borderRadius } from './styles';
import * as components from './components';

export default {
  space,
  colors,
  typography,
  body,
  zIndex,
  breakpoints,
  borderRadius,
  ...components,
};
