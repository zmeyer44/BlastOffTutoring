import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import Button from '@pagerland/common/src/components/Button';
import Icon from '@pagerland/common/src/components/Icon';

import Comparison from '@pagerland/icons/src/line/Comparison';
import Heart from '@pagerland/icons/src/line/Heart';
import Postcard from '@pagerland/icons/src/line/Postcard';

import Theme from '../../index';

<Meta title="Themes/Landing/Components/Button" component={Button} />

# Button

Buttons allow users to take actions, and make choices, with a single tap.
They are also prepared to working as a custom urls or route links.

```js
import Button from '@pagerland/common/src/components/Button';

<Button color="primary">Primary</Button>;
```

### Basic

<Preview>
  <Story name="Basic">
    <Theme>
      <Button>Default</Button>
      <Button color="primary">Primary</Button>
      <Button color="secondary">Secondary</Button>
      <Button color="accent">Accent</Button>
      <Button color="neutral">Neutral</Button>
    </Theme>
  </Story>
</Preview>

### Variants

<Preview>
  <Story name="Variants">
    <Theme>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="accent">Accent</Button>
      <Button variant="neutral">Neutral</Button>
    </Theme>
  </Story>
</Preview>

### Disabled

<Preview>
  <Story name="Outlined">
    <Theme>
      <Button disabled variant="primary">
        Primary
      </Button>
      <Button disabled variant="secondary">
        Secondary
      </Button>
      <Button disabled variant="accent">
        Accent
      </Button>
      <Button disabled variant="neutral">
        Neutral
      </Button>
    </Theme>
  </Story>
</Preview>

### Flat

<Preview>
  <Story name="Flat">
    <Theme>
      <Button flat variant="primary">
        Primary
      </Button>
      <Button flat variant="secondary">
        Secondary
      </Button>
      <Button flat variant="accent">
        Accent
      </Button>
      <Button flat variant="neutral">
        Neutral
      </Button>
    </Theme>
  </Story>
</Preview>

### Sizes

<Preview>
  <Story name="Sizes">
    <Theme>
      <Button size="small" variant="primary">
        Small
      </Button>
      <Button size="normal" variant="primary">
        Normal
      </Button>
      <Button size="large" variant="primary">
        Large
      </Button>
    </Theme>
  </Story>
</Preview>

### Icon button

<Preview>
  <Story name="Icon button">
    <Theme>
      <Button icon variant="primary">
        <Icon icon={Comparison} />
      </Button>
    </Theme>
  </Story>
</Preview>

### Customizing

<Preview>
  <Story name="Customizing">
    <Theme>
      <Button as="a" href="https://mypags.app" variant="primary">
        This is a link button
      </Button>
      <Button bg="shades.primary.2" color="gray.6">
        Custom color from defined palette
      </Button>
      <Button bg="#f50" color="gray.6">
        Custom hex color
      </Button>
    </Theme>
  </Story>
</Preview>

## Props

It supports also styled-system props.

<Props of={Button} />
