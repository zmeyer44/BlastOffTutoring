import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import Theme from '../../index';
import Logo from '../../components/Logo';

<Meta title="Themes/MobileApp/Components/Logo" component={Logo} />

# Logo

Your logo component.

```js
import Logo from '@pagerland/themes/src/MobileApp/components/Logo';

<Logo />;
```

<Preview>
  <Story name="Default">
    <Theme>
      <Logo />
    </Theme>
  </Story>
</Preview>

## Props

This component doesn't have specified prop types. Use default props as in svg html tag and styled system stuff.
